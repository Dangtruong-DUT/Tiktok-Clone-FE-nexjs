import VideoScrollWrapper from "@/app/[locale]/(public)/(home)/(foryou)/video-scroll-wrapper";

export default function HomePage() {
    return (
        <div className="pe-4 h-screen flex ">
            <VideoScrollWrapper />
        </div>
    );
}
