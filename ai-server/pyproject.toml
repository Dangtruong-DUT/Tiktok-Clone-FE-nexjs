[project]
name = "ai-server-training"
version = "0.1.0"
description = "EN+VI multi-stage toxic multi-label training pipeline"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.black]
line-length = 100
target-version = ["py310"]
